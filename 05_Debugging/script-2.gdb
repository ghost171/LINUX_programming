set pagination off

break 39 if $i >= 28 && $i <= 35
	command 1
	printf "@@@%d, %d, %d, %d\n", N, M, S, i
	continue
end

break 45 if $i >= 28 && $i <= 35
	command 1
	printf "@@@%d, %d, %d, %d\n", N, M, S, i
	continue
end

break 50 if $i >= 28 && $i <= 35
	command 1
	printf "@@@%d, %d, %d, %d\n", N, M, S, i
	continue
end

run -100 100 3

quit